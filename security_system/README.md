## iot охранная система / esp32 + pir + telegram + web ui (wokwi)

### схема и компоненты в wokwi
- [ ] добавить esp32 на схему
- [ ] добавить pir сенсор (hc-sr501) пин 13 
- [ ] добавить led (индикатор тревоги) пин 2 
- [ ] добавить buzzer пин 15 
- [ ] добавить кнопку ручного сброса тревоги пин 4 
- [ ] прописать `diagram.json` с правильными соединениями
- [ ] добавить `libraries.txt` с зависимостями

### прошивка ядро системы
- [ ] подключение к wi-fi (wokwi-guest)
- [ ] реализовать машину состояний: `idle → armed → alarm → cooldown`
- [ ] логика pir: фиксировать фронт low→high, избегать дублей
- [ ] реализовать cooldown таймер (не спамить уведомления)
- [ ] управление led и buzzer при тревоге
- [ ] кнопка физического сброса тревоги с debounce
- [ ] хранить состояние (`armed`, `alarm`, `cooldown`) в переменных

### telegram бот
- [ ] подключить `UniversalTelegramBot` + `WiFiClientSecure`
- [ ] polling новых сообщений раз в 3 сек (неблокирующий)
- [ ] команда `/arm` взять под охрану
- [ ] команда `/disarm` снять с охраны
- [ ] команда `/status` текущее состояние системы
- [ ] команда `/help` список команд
- [ ] фильтрация по chat_id (игнорировать чужих)
- [ ] уведомление при тревоге с временной меткой
- [ ] уведомление при постановке/снятии с охраны

### веб-сервер (локальный ui)
- [ ] `/` страница статуса (armed/disarmed, последнее событие)
- [ ] `/arm` и `/disarm` управление через браузер
- [ ] `/status` json endpoint (`{"armed":true,"alarm":false}`)
- [ ] минималистичный html ui (тёмная тема, monospace)
- [ ] отображать ip адрес в serial при старте
